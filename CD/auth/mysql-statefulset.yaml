apiVersion: apps/v1
kind: StatefulSet

metadata:
  name: db-shopflow-auth
  namespace: dev

spec:
  minReadySeconds: 3
  replicas: 1
  
  serviceName: db-shopflow-auth

  selector:
    matchLabels:
      app: db-shopflow-auth
  
  template:
    metadata:
      labels: 
        app: db-shopflow-auth

    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          imagePullPolicy:  IfNotPresent

          ports:
            - containerPort: 3306
              protocol: TCP
          
          env:
            - name: MYSQL_DATABASE
              value: shopflow_auth

            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: auth-backend-service-secret
                  key:  DB_PASSSWORD 

          volumeMounts:
            - name: auth-db-storage
              mountPath: /var/lib/mysql
  
  volumeClaimTemplates:
    - metadata: 
      name: auth-db-storage

      spec:
        storageClassName: shopflow-auth-ebs
        
        accessModes:
          - ReadWriteOnce
        
        resources:
          requests:
            storage:  2Gi
