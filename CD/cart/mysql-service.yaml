apiVersion: v1
kind: Service

metadata:
  name: db-shopflow-carts
  namespace: dev

spec:
  type: ClusterIP
  clusterIP: None

  selector:
    app: db-shopflow-carts

  ports:
    - port: 3306
      nodePort: 3306
      protocol: TCP
