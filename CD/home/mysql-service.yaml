apiVersion: v1
kind: Service

metadata:
  name: db-shopflow-home
  namespace: dev

spec:
  type: ClusterIP
  clusterIP: None

  selector:
    app: db-shopflow-home

  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
