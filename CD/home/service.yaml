apiVersion: v1
kind: Service
metadata:
  name: home-backend-service-svc-dp
  namespace: dev
spec:
  type: NodePort
  selector:
    app: home-backend-service-app
  ports:
    - port: 8001
      targetPort: 8001
      protocol: TCP
      nodePort: 30001