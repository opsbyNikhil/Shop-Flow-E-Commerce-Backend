apiVersion: v1
kind: Service

metadata:
  name: db-shopflow-orders
  namespace: dev

spec:
  clusterIP: None

  selector:
    app: db-shopflow-orders

  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
