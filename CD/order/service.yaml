apiVersion: v1
kind: Service
metadata:
  name: order-backend-service-svc-dp
  namespace: dev
spec:
  type: NodePort
  selector:
    app: order-service-app
  ports:
    - port: 8004
      targetPort: 8004
      protocol: TCP
      nodePort: 30003