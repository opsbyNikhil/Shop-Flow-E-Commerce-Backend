apiVersion: v1
kind: Service

metadata:
  name: db-shopflow-products
  namespace: dev

spec:
  type: ClusterIP
  clusterIP: None

  selector:
    app: db-shopflow-products

  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
