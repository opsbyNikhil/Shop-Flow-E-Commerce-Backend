apiVersion: v1
kind: Service
metadata:
  name: product-backend-service-svc-dp
  namespace: dev
spec:
  type: NodePort
  selector:
    app: product-service-app
  ports:
    - port: 8002
      targetPort: 8002
      protocol: TCP
      nodePort: 30002