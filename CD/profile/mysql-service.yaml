apiVersion: v1
kind: Service

metadata:
  name: db_shopflow_profiles
  namespace: dev

spec:
  clusterIP: None

  selector:
    app: db_shopflow_profiles

  ports:
    - port: 3306
      nodePort: 3306
      protocol: TCP
