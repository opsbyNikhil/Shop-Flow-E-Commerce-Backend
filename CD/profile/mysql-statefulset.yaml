apiVersion: apps/v1
kind: StatefulSet

metadata:
  name: db_shopflow_profiles
  namespace: dev

spec:
  minReadySeconds: 3
  replicas: 1
  
  serviceName: db_shopflow_profiles

  selector:
    matchLabels:
      app: db_shopflow_profiles
  
  template:
    metadata:
      labels: 
        app: db_shopflow_profiles

    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          imagePullPolicy: IfNotPresent

          ports:
            - containerPort: 3306
              protocol: TCP
          
          env:
            - name: MYSQL_DATABASE
              value: shopflow_profiles

            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: profile-backend-service-secret
                  key:  DB_PASSSWORD
      
      volumeMounts:
        - name: profile-db-storage
          mountPath: /var/lib/mysql
  
  volumeClaimTemplates:
    - metadata: 
      name: profile-db-storage

      spec:
        storageClassName: shopflow-profile-ebs
        
        accessModes:
          - ReadWriteOnce
        
        resources:
          requests:
            storage:  2Gi
