apiVersion: v1
kind: Service
metadata:
  name: profile-backend-service-svc-dp
  namespace: dev
spec:
  type: NodePort
  selector:
    app: profile-service-app
  ports:
    - port: 8004
      targetPort: 8004
      protocol: TCP
      nodePort: 30004